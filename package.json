{
  "name": "AwsManagedAccount",
  "description":"Make use of AWS Organisation Function to create the managed AWS account.",
  "version": "0.0.1",
  "author": "Cyrus Wong",
  "license": "MIT",
  "dependencies": {
    "async": ">=2.x",
    "aws-sdk": ">=2.x",
    "cfn-response": ">=1.x",
    "s3": ">=4.x"
  },
  "devDependencies": {
    "aws-sdk": ">=2.x",
    "grunt": ">=0.4.*",
    "grunt-aws-lambda": ">=0.1",
    "grunt-pack": ">=0.1.5"
  }
}
